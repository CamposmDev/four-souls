package org.camposmdev.client.entity;

public enum EntityType {
	PLAYER,	/* Assigned to all players regardless if singleplayer or multiplayer will have this type */
	COPLAYER    /* Assigned to second players in solitaire mode */
}
